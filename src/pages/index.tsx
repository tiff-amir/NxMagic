import React, { FC } from 'react';

import Root from '@container/_pages/Root';
import PageWrapper from '@presentation/PageWrapper';

const IndexPage: FC = () => {
  return (
    <PageWrapper>
      <Root />
    </PageWrapper>
  );
};

export default IndexPage;
